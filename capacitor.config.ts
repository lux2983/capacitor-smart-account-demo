import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lux2983.smartaccountdemo',
  appName: 'Smart Account Demo',
  webDir: 'dist',
};

export default config;
